import primes
from primes import listPrimes # can also import individual functions
# a module is essentially a file in python
# package is essentially a folder in python
# a package is a collection of modules (see numbers folder)

print(primes.isPrime(5))
print(listPrimes(10))